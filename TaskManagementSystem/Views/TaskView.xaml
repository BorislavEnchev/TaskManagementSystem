<Window x:Class="TaskManagementSystem.Views.TaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TaskManagementSystem.ViewModels"
        Title="Task Management" Height="350" Width="525">
    <Grid>
        <ListView ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}">
            <!-- Define columns for Task properties -->
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}"/>
                    <GridViewColumn Header="Due Date" DisplayMemberBinding="{Binding RequiredByDate}"/>
                    <!-- Add more columns as needed -->
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Orientation="Vertical" Margin="100">
            <Button Command="{Binding LoadTasksCommand}">Load Tasks</Button>
            <Button Command="{Binding CreateTaskCommand}">Create Task</Button>
            <Button Command="{Binding UpdateTaskCommand}">Update Task</Button>
            <Button Command="{Binding DeleteTaskCommand}">Delete Task</Button>
        </StackPanel>
    </Grid>
</Window>