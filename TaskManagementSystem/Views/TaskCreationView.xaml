<Window x:Class="TaskManagementSystem.Views.TaskCreationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Task" Height="400" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Description:" Grid.Row="0"/>
        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Width="300"/>

        <TextBlock Text="Required By Date:" Grid.Row="2"/>
        <DatePicker SelectedDate="{Binding RequiredByDate}" Grid.Row="3"/>

        <TextBlock Text="Status:" Grid.Row="4"/>
        <ComboBox ItemsSource="{Binding TaskStatuses}" SelectedItem="{Binding Status, Mode=TwoWay}" Grid.Row="5"/>

        <TextBlock Text="Task Type:" Grid.Row="6"/>
        <ComboBox ItemsSource="{Binding TaskTypes}" SelectedItem="{Binding Type, Mode=TwoWay}" Grid.Row="7"/>

        <TextBlock Text="Assigned To:" Grid.Row="7" Margin="0,27,0,-27"/>
        <ComboBox ItemsSource="{Binding Users}" SelectedItem="{Binding AssignedTo, Mode=TwoWay}" DisplayMemberPath="Name" Grid.Row="7" Margin="0,43,0,-43"/>

        <Button Content="Save Task" Command="{Binding SaveTaskCommand}" Width="100" Margin="190,77,190,-75" Grid.Row="7"/>
    </Grid>
</Window>
